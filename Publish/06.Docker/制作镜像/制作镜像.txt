#构建镜像

# docker login -u serset -p xxxxxxxxx

#把本文件所在目录下的文件夹拷贝到image 

cd /root/image
cd sqler


#构建镜像
docker build . -t serset/sqler:2.25.373 -t serset/sqler

#推送到镜像仓库
docker push serset/sqler:2.25.373
docker push serset/sqler
 




#强制删除镜像名称中包含sqler的镜像
# docker rmi --force $(docker images | grep sqler | awk '{print $3}')








 